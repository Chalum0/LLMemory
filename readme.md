# LLMemory

This is a simple python demo of using embeddings to store and retrieve memories for LLMs.
Requirements:
- Python 3.8+
- LiteLLM Proxy running (or lm studio) with a model called EmbeddingGemma and a model of your choice to put in the script before running.
